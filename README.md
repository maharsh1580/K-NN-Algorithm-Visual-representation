# K-NN-Algorithm-Visual-representation
This repository implements the K-Nearest Neighbors (KNN) algorithm for classification using Python. It allows users to choose the value of K, calculates distances using the Euclidean metric, predicts classes by majority voting, and visualizes results with Matplotlib for better understanding.
